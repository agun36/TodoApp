# Local dev: Vite proxies /api → this URL (default is http://localhost:3000).
# Only set if your API runs on a different host/port.
# VITE_API_PROXY_TARGET=http://localhost:3000

# Production build only — set on Render/static hosting for the deployed frontend.
# VITE_API_URL=https://your-api.onrender.com
