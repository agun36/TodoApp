NODE_ENV=production
PORT=3000
# Generate a long random string — do NOT use your database URL here
SESSION_SECRET=replace-with-long-random-secret
# Set DATABASE_URL in the Render dashboard (Environment), not in this file.
# Use the Postgres Internal Database URL (host like dpg-xxxxx-a, not localhost).
